# labelrag

`labelrag` is a label-driven RAG pipeline built on top of `paralabelgen`.

Current project status:

- design documents are in `docs/`
- implementation is being developed incrementally
- the initial target dependency is `paralabelgen==0.2.0`

